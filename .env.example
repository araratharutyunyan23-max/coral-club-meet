# Shared local-development credentials for the LiveKit dev server.
# These match `livekit-server --dev`. NEVER use these values in production.
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret
LIVEKIT_URL=ws://localhost:7880
